# Ссылки на скачивание и сборка

## 📦 APK Не может быть собран автоматически

Для сборки Android APK требуется полный Android SDK и специфическая среда, которые недоступны в текущей среде. Однако я подготовил несколько вариантов получения APK:

## 🎯 Варианты получения APK

### Вариант 1: Сборка через GitHub Actions (Рекомендуется)

**Шаг 1: Создайте репозиторий на GitHub**
1. Зайдите на https://github.com/new
2. Создайте новый репозиторий "ai-keyboard"
3. Загрузите файлы проекта

**Шаг 2: Настройте GitHub Actions**
```
.gitignore
.github/workflows/build-apk.yml
```

**Шаг 3: Скачайте APK**
- Перейдите в Actions вашего репозитория
- Запустите "Build Android APK"
- Скачайте готовый APK из artifacts

### Вариант 2: Самостоятельная сборка через Android Studio

**Требуется:**
- Android Studio (скачать: https://developer.android.com/studio)
- Java JDK 17+ (скачать: https://adoptium.net/)

**Инструкция:**
1. Установите Android Studio
2. Откройте папку `android/` в Android Studio
3. Дождитесь загрузки Gradle
4. Выберите: Build → Build Bundle(s) / APK(s) → Build APK(s)
5. APK будет в: `android/app/build/outputs/apk/debug/app-debug.apk`

### Вариант 3: Сборка через командную строку

#### Windows:
```batch
build_apk.bat
```

#### Linux/Mac:
```bash
./build_apk.sh
```

## 📥 Где найти APK после сборки

После успешной сборки:
```
ai-keyboard-debug.apk (в корне проекта)
или
android/app/build/outputs/apk/debug/app-debug.apk
```

## 📲 Установка APK на телефон

### Способ 1: Через ADB (Android Debug Bridge)
```bash
adb install ai-keyboard-debug.apk
```

### Способ 2: Через USB/Google Drive
1. Скопируйте `ai-keyboard-debug.apk` на телефон
2. Откройте файл
3. Разрешите установку из неизвестных источников
4. Нажмите "Установить"

## 🚀 После установки

1. **Активируйте клавиатуру:**
   - Настройки → Язык и ввод → Текущая клавиатура
   - Выберите "AI Keyboard"

2. **Запустите Backend:**
   ```bash
   start_backend.bat  # Windows
   # или
   cd backend && npm start
   ```

3. **Используйте приложение!**

## 📊 Размер APK

Примерный размер: 5-15 MB (зависит от конфигурации)

## 🔧 Если сборка не удается

### Проблема: ANDROID_HOME не найден
**Решение:**
```bash
# Windows
setx ANDROID_HOME "C:\Users\<имя>\AppData\Local\Android\Sdk"

# Linux/Mac
export ANDROID_HOME=~/Android/Sdk
```

### Проблема: Java не найдена
**Решение:** Установите Java JDK 17 с https://adoptium.net/

### Проблема: Build Tools не установлены
**Решение:** Откройте SDK Manager в Android Studio и установите:
- Android SDK Build-Tools 34.0.0
- Android SDK Platform 34

### Проблема: Лицензии не приняты
**Решение:**
```bash
sdkmanager --licenses
```

## 🌐 Дополнительные ресурсы

- **Android Studio:** https://developer.android.com/studio
- **Java JDK:** https://adoptium.net/
- **ADB Tools:** https://developer.android.com/studio/releases/platform-tools
- **Полная документация:** COMPILE.md

## 💡 Быстрый старт для разработчиков

Если у вас уже настроена среда Android:
```bash
cd android
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

## 📝 Примечание

- APK собирается как debug (без оптимизации)
- Для production нужна подпись ключом
- Backend работает независимо от Android приложения
- Можно использовать приложение без backend (с mock предсказаниями)

## 🎉 Удачи!

Если возникнут вопросы, пишите в issues или создавайте pull requests!